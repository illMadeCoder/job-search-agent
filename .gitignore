# ============================================
# Personal Data (NEVER commit)
# ============================================

# LinkedIn data export
linkedin_archive/
connections.csv

# Gmail OAuth credentials
credentials.json
gmail-tokens*.json

# Your personal config (template is tracked)
config.yaml

# Your actual resume (template ok, personal content not)
jb_resume_*.tex
jb_resume_*.pdf

# ============================================
# Job Search Data (contains applications/PII)
# ============================================

# Actual postings (keep schemas/templates)
postings/*/

# Actual digests (keep schemas/templates)
digest/*.yaml
!digest/_schema.yaml
!digest/_template.yaml

# ============================================
# Local State
# ============================================

# Beads database (agent memory)
.beads/

# Python virtual environment
.venv/

# Logs (keep observations for pattern detection)
logs/
!logs/.gitkeep
!logs/observations.jsonl

# ============================================
# Editor/OS files
# ============================================
*.swp
*~
.DS_Store
*.Zone.Identifier
